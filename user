---
- hosts: dbserver
  become_user: root
  become_method: sudo
  tasks:
   - name: Create a new user
     user: 
       name: Jack
       state: present
   - name: create user Khaja
     user:
       name: Khaja
       state: present